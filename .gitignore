# Rust files and build artifacts
/target/
**/*.rs.bk

# Dev-run install-tree output: `.cargo/config.toml` anchors `cargo run` to the
# repo root, so running the tool here creates per-character gear folders.
/*_Gear/

# OS and IDE files
.DS_Store
Thumbs.db
*.swp

# VSCode
.vscode/

# Ultra-Edit project files
LGO_project.prj
LGO_project.SURROGATE.User.pui
