# PR 1 — Refuse-on-Overflow Guard (coding-agent prompt)

**Repo:** ThalyaFlourishing/LGO  **Branch:** cut a feature branch from `main`.
**Model guidance:** `optimizer.rs` control-flow is frontier-model territory
(`docs/MODEL_GUIDANCE.md`). This PR changes `optimize`'s control flow and return
type — treat it accordingly.

## Context
`MAX_CANDIDATES_PER_SLOT = 8` is being redefined from an internal **truncation**
heuristic into a **supported-input contract**: if any single slot OR any paired
family (Wrist / Finger / Ear) has more than 8 owned candidates, the optimizer
must **refuse** (clear, actionable error) rather than silently truncating.
See `docs/Optimizer_Overhaul/07 - Locked Semantics and Rewrite Plan.md` §3.

This is PR 1 of 2. It does **only** the guard. It must **not** change the search
algorithm or the objective (that is PR 2). Keep this PR small and low-risk.

## Required changes

### 1. `src/optimizer.rs` — replace truncate-and-warn with refuse
- **Delete** the truncation block (currently ~lines 151–162: the
  `pool.len() > MAX_CANDIDATES_PER_SLOT` ? warning + `pool.truncate(...)`).
- Add an error type:
  ```rust
  #[derive(Debug, Clone, PartialEq, Eq)]
  pub enum OptimizeError {
      TooManyCandidates { slot_label: String, count: usize, max: usize },
  }
  ```
  Implement `std::fmt::Display` (and `std::error::Error`) with an actionable
  message, e.g.:
  `Too many candidates for slot "Finger": 11 provided, maximum allowed is 8. Remove items from the 'lgo' chest and re-export before running 'lgo optimize'.`
- Change the entry point signature to:
  `pub fn optimize(...) -> Result<OptimizeResult, OptimizeError>`
- **Validation timing & scope:** after grouping items into per-canonical-slot
  pools (i.e. after `canonical_slot` folding, so Wrist1/Wrist2 count as ONE
  "Finger/Wrist/Ear" family), and **before** building pairs / any search:
  - For each single slot, if `pool.len() > MAX_CANDIDATES_PER_SLOT` ? return
    `Err(TooManyCandidates{...})` using the human slot label (reuse
    `slot_display`).
  - For each paired **family** (Wrist, Finger, Ear), validate the **family pool
    size** (the pre-pair count of owned items in that family), NOT the number of
    generated pairs. If it exceeds the cap ? `Err(...)`.
  - Report the FIRST offending slot/family in a deterministic order (iterate
    `Slot::ALL`, mapping through `canonical_slot`, so the message is stable).
- The `warnings` field remains for other warnings (e.g. empty-slot
  placeholders); only the truncation warning is removed.

### 2. `src/main.rs` — handle the new Result
- `run_optimize` currently does `let result = optimizer::optimize(...);`
  (~line 144). Change to match on the `Result`:
  - `Ok(result)` ? proceed to `report::print_report` as today.
  - `Err(OptimizeError::TooManyCandidates{ slot_label, count, max })` ?
    `eprintln!` the actionable message and `process::exit(1)`. Include the same
    "remove items from the 'lgo' chest and re-export" guidance.

### 3. `src/lgo.lua` — lightweight total-count sanity warning ONLY
**Important API limitation (verified):** the plugin exports item **names only**
and does NOT know the optimizer slot of Shared Storage (chest) items. It
therefore **cannot** reliably count candidates per optimizer slot-family at
export time. Do **not** attempt per-family refusal in Lua — it would miss the
chest (where overflow actually happens) and give false confidence.

Instead:
- Add a named constant near the top of the file:
  `local MAX_CANDIDATES_PER_SLOT = 8;  -- keep in sync with Rust optimizer`
- After building the combined export list in `ExportCombined`, compute the total
  owned-instance count (`#out.names`). As a **soft heuristic** (there are 19
  slots, so a full set is ~19–22 items), if the total is large enough that
  per-slot overflow is plausible, print a **non-blocking warning** — e.g.:
  `Print("NOTE: exported N items. The optimizer allows at most " .. MAX_CANDIDATES_PER_SLOT .. " candidates per slot/family. If a single slot exceeds that, 'lgo optimize' will refuse until you remove some. Rust performs the authoritative check.")`
- Do **not** block the export. The Rust guard is authoritative.
- Document (a comment) that a precise Lua-side per-family check is deferred and
  would require slot-classification the current API does not provide.

## Tests
### Rust (`src/optimizer.rs` test module)
- **DELETE** `test_truncation_warning_emitted_for_oversized_pool` — it asserts
  the now-removed truncation behavior.
- **ADD** `test_too_many_single_slot_candidates_is_refused`: 9 Head items ?
  `optimize(...)` returns `Err(TooManyCandidates{ slot_label: "Head", count: 9,
  max: 8 })`.
- **ADD** `test_too_many_paired_family_candidates_is_refused`: 9 Finger items
  (mix of Finger1/Finger2 is fine) ? `Err(TooManyCandidates{ slot_label:
  "Finger", count: 9, max: 8 })`. This confirms the check is on the FAMILY pool,
  not per-slot or per-pair.
- **ADD** `test_exactly_eight_candidates_is_allowed`: 8 Head items ? `Ok(_)`
  (boundary: 8 is allowed, 9 is not).
- **ADD** `test_eight_per_family_paired_is_allowed`: 8 Finger items ? `Ok(_)`.
- Every OTHER existing optimizer test must be updated **only** to unwrap the new
  `Result` (e.g. `optimize(...).unwrap()` / `.expect("...")`). Do **not** change
  their expected outcomes — the search behavior is unchanged in this PR.

### `src/main.rs`
- No new parse tests needed; ensure the crate still compiles and existing tests
  pass after the `Result` handling change.

## Acceptance criteria
- `cargo build`, `cargo test`, `cargo clippy`, `cargo fmt --check` all clean.
- 9-in-a-slot and 9-in-a-family cases return the refusal error; 8 is accepted.
- No truncation anywhere; the truncation warning string is gone.
- Lua export still succeeds and prints the soft NOTE for large exports.
- The search algorithm and objective are **unchanged** (deferred to PR 2).

## Out of scope (do NOT do here)
- The objective-function rewrite, dominance/branch-and-bound, oracle, or fuzzer.
- Report wording changes for the new objective (PR 2).
- Any change to slot grouping, `build_pairs`, or candidate identity.